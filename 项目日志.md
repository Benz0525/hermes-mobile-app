# Hermes Mobile App — 项目日志

---

## 2026-06-01 v5.4.0 大版本 — 6大模块

### F. v5.3.x 收尾
- OverrideEditor.js: tempStep 0.1→0.05, round *10→*20, toFixed(1)→(2)

### A. UI 极客风重设计
- ConversationItem.js: 40px outline头像 + 模型徽章(DS/QW/GL/VL色点) + 字体三级(白/60%灰/40%灰)
- ChatListScreen.js: 日期分组(今天/昨天/本周/更早)，caps+letterSpacing
- dateFormat.js: 新建，formatDate() + dateGroup()

### B. 会话交互
- ActionSheet.js: 新建，底部弹出菜单(删除/重命名/置顶)
- SwipeableRow.js: 新建，PanResponder左滑删除
- ChatListScreen: 集成B1+B3+B4，未读badge(AsyncStorage hermes_last_seen)，置顶isPinned

### C. 消息分页
- ChatScreen: allMessagesRef全量 + 首屏slice(-20)
- FlatList inverted=true + onEndReached loadMoreOlder
- 性能参数: initialNumToRender=20, maxToRenderPerBatch=10, windowSize=5, removeClippedSubviews

### D. 抽屉菜单
- DrawerMenu.js: 新建，Modal+Animated左侧滑入280px，零新增依赖
- ChatListScreen: ☰ 按钮替换 ⚙，抽屉项(新建对话/定时任务/设置/关于)

### E. 定时任务系统（后端全新）
- server.py: +apscheduler +sqlite3 +lifespan
- tasks + task_runs 两张表
- 5路由: GET/POST/PUT/DELETE /tasks + GET /tasks/{id}/runs
- seed 2条: 每日早报(id=61aa2a6fd0f9) + 每周AI学习卡片(id=4f7aa3750044)
- requirements.txt: +apscheduler

### E. 定时任务系统（前端）
- TasksScreen.js: 任务列表 + 最近运行 + FAB新建
- TaskFormScreen.js: 创建/编辑(name/prompt/cron/model/enabled)
- App.js: 注册 Tasks + TaskForm 路由

### 版本号四件套
- app.json: 5.4.0 / versionCode=34
- package.json: 5.4.0
- server.py: VERSION=5.4.0 / VERSION_CODE=34
- src/constants/version.js: 动态读app.json，无需改

### 下一步
- git push → Actions 编译 APK
- 装 apscheduler → 重启 hermes-mobile-api
- 部署 APK 到 /var/www/html/hermes-chat.apk
- 验证: /tasks 返回 2 条 seed + /version 显示 5.4.0
